INICIALIZAR LOCALMENTE (IntelliJ IDEA):

    Por interfaz de IntelliJ crear New poryecto
    
    Abrir el proyecto desde backend/.

    Agregar las siguientes dependencias en Spring Initializr o desde la UI:

        Spring Web → Web > Spring Web

        Spring Data JPA → SQL > Spring Data JPA

        PostgreSQL Driver → SQL > PostgreSQL Driver

        (Opcional) Spring Boot DevTools


HOST:
  http://localhost:8080
